---
purpose: Provenance/staleness matrix + chart quality audit of the tiered bot's full strategy stack, with a prioritized re-validation plan. Output of the 2026-06-12 read-only audit workflow.
type: design
created: 2026-06-12
last_updated: 2026-06-12
---

> Generated by a 9-agent read-only audit (strategy-revalidation-audit workflow). 26 layers + 17 charts.
> Trigger: most shipped EV verdicts predate (a) the 2026-06-11 human_clone fold-to-3bet fix
> (every prior clone A/B used a NON-folding clone) and (b) the preflop chart regen (#295/#299/#300).
> Treat field-relative EV claims as UNVERIFIED on current code until re-run per the plan below.

# Strategy Stack Re-Validation Audit (2026-06-11)

## 1. Honest Summary

Most of the shipped strategy stack's EV claims rest on stale or never-measured results. Of the ~25 runtime layers and 18 charts audited, the load-bearing ones — `_apply_exploitation` (+22.5 bb/100 headline), the entire postflop personality/spot-tendency stack, the multistreet barrel, value/induce overrides, and position-blindness — were all last validated in late May 2026 against a `human_clone` that **never folded to 3-bets or jams** (fixed only on 2026-06-11, commit `5d021844`) and/or **before the 2026-06-11 preflop chart regen** (#295 vs_open ratchet, #299 per-player bluff-aware vs_3bet, #300 nit/rock VPIP). Every fold-equity-dependent feature (barrel, river bluff, overbet de-face-up, reshove) was therefore priced against a non-folder, systematically understating fold equity — meaning the "+EV" numbers are optimistic and the "no-leak" verdicts may not hold. The genuinely trustworthy results are narrow: the HU push/fold Nash chart (correctness is mathematical, not field-relative), the gated reshove (validated same-day post-clone-fix — the one layer measured *against* a folder), and the dormant/off layers (zero live risk). A large fraction of the stack is also **unmeasured by construction**: the relationship modifier (defaults ON, zero EV measurement ever), the preflop spot-tendency variant (no isolating A/B), and all the depth/postflop charts (un-linted, never solver-derived). The honest posture: treat the field-relative EV stack as **unverified on current code**, not "validated and trusted."

## 2. Staleness Matrix

| Layer | Gating / On? | Claim | Last validated | Staleness | Priority | Instrument |
|---|---|---|---|---|---|---|
| `_apply_exploitation` | ON (any sharp bot w/ manager); most sub-rules dormant | +22.5 bb/100 vs CallStation; net +EV not just fish-milking | 2026-05-27, CRN exploit_bb100 vs **non-folding** station clone | **high** | **high** | exploit_bb100 |
| `_apply_relationship_modifier_to_offsets` | ON (default True); bites only w/ accrued heat | Believability only; mutates EV-critical offsets pre-clamp | **never EV-measured** (unit tests only) | **high** | **high** | exploit_bb100 |
| `_layer_multistreet_context` (H1 barrel) | ON for every sharp bot (init True despite "default off" docstring) | +3.33 [+0.14,+6.52] vs jeff, +11.94 vs station | 2026-05-27, node-attribution vs **non-folding** clone | **high** | **high** | simulate_bb100+probe |
| `_layer_overbet_context` (value overbets) | ON for every sharp bot (init True despite docstring) | +13 vs reg … +159 vs station; never negative | 2026-05-28, node-attribution vs **non-folder**; −22 bb/100 known vs sizing-reader | **high** | **high** | ab_node_attribution / simulate_bb100+probe |
| `_layer_overbet_context` (river_bluff/T2) | ON, regime-gated (fold_to_big_bet≥0.6) | De-face-ups river −28→−7 bb/100; value-only vs callers | 2026-06-01, **synthetic oracle** + pre-fix clones | **high** | **high** | ab_node_attribution / simulate_bb100+probe |
| `_layer_spot_tendencies` (postflop) | ON for archetypes w/ tendencies (e.g. weak_fish) | Archetype believability; over_bluff "free vs callers" | 2026-05-29/30, variety_eval, **old codebase** | **high** | **high** | exploit_bb100 |
| `_layer_preflop_spot_tendencies` | ON for personas w/ preflop tendency (defend_3bet) | Believability shaping; inherited EV | **never isolated** (rides framework) | **high** | **high** | exploit_bb100 |
| `_apply_position_blindness` (preflop) | ON only for $2 weak_fish | Real fish leak −13.5 @40bb / −8.4 @100bb | 2026-05-29/30, **oldest** validation, foldy field | **high** | **high** | simulate_bb100+probe |
| `_apply_value_override` (preflop variant) | ON; postflop dormant | LAG +56 / Maniac −179 (v1) — undated | predates vs_4bet fix + chart regen | **high** | **high** | champion_challenger |
| `_layer_tilt_conditioning` | ON (STABLE), effective only for maniac in tilt | Believability + EV-safety (cap-clamped); no live bb/100 | 2026-06-11 (indirect: cap probe + reachability) | **high** | **high** | simulate_bb100+probe / archetype_mixedfield_probe |
| `_apply_math_floor` (call-off-all-in fix) | ALWAYS ON | Forces +EV call/jam in committed/short/trivial spots | call-off branch new 2026-06-11, **unit-test only** | **high** | **high** | exploit_bb100 + sng_runner |
| `_try_push_fold_6max` (unopened + caller) | 8 sharks, ≤15BB; default OFF live | Routing-coverage only; bb/100 **TODO** | 2026-06-11 coverage read; **no bb/100** | **high** | **high** | sng_runner |
| `reshove_action_6max` + FE gate | Triple-gated (flag + 8 sharks + FE read); prod ON | Pareto-safe: +0.0 vs mix, −1.2/+0.7/+2.7 vs folder | 2026-06-11, **post-clone-fix** (the good one) | med | med | simulate_bb100+probe |
| `_apply_vs3bet_bluff_exploit` | ON field-wide @0.5 in prod; no-ops in sims | Nearly risk-free EV (fixed villain range) | 2026-06-11, **worked example only**, no bb/100 | med | med | exploit_bb100 |
| `_apply_bluff_catch_override` | ALWAYS ON; EXTREME-tier gated | +EV but rare (~0.25% hands) vs over-bluffers | ~2026-05-28, vs rule-bot maniacs; sibling 0/26 LLM | med | med | exploit_bb100 |
| `_layer_defense_floor` | ALWAYS ON; deferred by priors | Correctness floor; rejected jam row −3.5 bb/100 | 2026-05-25, 1000x5 sim, pre-regen | med | med | simulate_bb100+probe |
| `_apply_induce_override` | ON; fires ~9/72k hands | Correctness-only; one branch read −2.39 | late-May; below noise / negative | med | med | simulate_bb100+probe |
| TILT_ERRATIC_READS_ENABLED | OFF (experimental) | Mildly +EV vs off (restores read) | 2026-06-10, factor-level probe only | med | med | exploit_bb100 + tilt corpus |
| TILT_SIGNATURE_ENABLED | OFF (experimental) | EV-safety; no catastrophic trash-shove | 2026-06-09/10, probe vs **old charts** | med | med | simulate_bb100+probe |
| `_apply_sizing_defense` | OFF (no persona opts in) — dormant | +4.27 bb/100 CI [−8.2,+16.7] (∋0), marginal | 2026-06-03, pre-regen; CI spans zero | low | low | simulate_bb100+probe |
| `_try_push_fold_hu` (router + HU) | 8 sharks, HU, ≤15BB | GTO-perfect HU Nash (correctness claim) | 2026-05-26, vs HUNE anchors | low | low | sng_runner |
| `_apply_postflop_position_blindness` | ON only $2 weak_fish | Intentionally −EV fish leak | ~2026-05-27, calling-station clone (fine) | low | low | simulate_bb100 |
| `_layer_multistreet_context` (H2 fold-barrel) | OFF | Inert / slightly −EV; kept off | 2026-05-27, pre-CRN | med | low | simulate_bb100+probe |
| `_layer_multistreet_context` (air_barrel) | OFF (target 0.0) | Unproven / measure-first | never (designed 2026-06-01) | low | low | measure_passivity |
| TILT_PERSISTENCE_ENABLED | OFF | Duration believability; marginal effect unmeasured | trajectory-desynced sim | low | low | simulate_bb100+probe |
| TILT_TELEGRAPH_ENABLED | OFF; frequency-neutral | Perceptibility only (no EV by construction) | n/a | low | low | manual |

## 3. Chart Audit

| Chart | Completeness gaps | Semantic concerns | Overridden by | Priority |
|---|---|---|---|---|
| `postflop_strategies.json` (LIVE) | Only (SRP, high-SPR); no aggressor flag, no 3BP/low-SPR/limped trees | air c-bets 53% unopened (donk/c-bet conflation = live leak); memoryless; validation predates clone-fix | HEAVILY — full 7-layer pipeline; overbet/multistreet/math_floor fire broadly | **high** |
| `preflop_100bb_6max.json` (BASE) | No 5-bet/multiway/limped/straddle; depth offloaded | vs_3bet/vs_4bet equity-derived (least-confident); **stale "+17bb/100" meta** predates #295/#299/#300 | HEAVILY — 6 preflop layers + push/fold + veto at ≤15bb | **high** |
| `preflop_100bb_6max_loose.json` (maniac) | vs_squeeze missing; no depth variants | re-raise mass floors ~29-30%, can't hit ~20-25% target via cap; shared across 3 archetypes | 6 preflop layers; reraise split pulls 3-bet down; tilt_conditioning | **high** |
| `preflop_100bb_6max_loose_mid.json` (lag) | vs_squeeze missing; no depth | KNOWN leak: vs_3bet mass too high; likely AT/OVER fourbet_band — **but lint never runs on it** | 6 layers; reraise_scale 0.45 + max_shift 0.10 | **high** |
| `preflop_25bb_6max.json` | No vs_squeeze; single raise size; one snapshot spans >15–37bb | **BB defense 100% jam / 0% flat at 25bb = generator artifact**, theory-disfavored; wide post-#295 RFI at depth UNMEASURED | Heavily shadowed: push/fold owns ~98% at 10bb; all-in veto moots jam cells | **high** |
| `postflop_strategies.json` un-lint gap | lints.py is PREFLOP-ONLY — 0 postflop coverage | (see live row) — only loader sum~1.0 check exists | — | **high** |
| `preflop_100bb_6max_station.json` | vs_squeeze missing; no depth | Nit-width opens (RFI 17.6%) but 63% flat — counterintuitive; vs_3bet continue 43% exploitably sticky | 6 layers; flats survive (no reraise split on station) | med |
| `preflop_100bb_6max_weak_station.json` | vs_squeeze missing; no depth | vs_3bet continue 33.9% < station's 43.1% — **inverted** (weaker fish less sticky?); verify generator | 6 layers + heavy weak_fish loadout + floors | med |
| `preflop_100bb_6max_tight_rfi.json` (nit/rock) | vs_squeeze missing; no depth | Flats WIDER than base (31.9% vs 27.9%) — damped premiums into flats; VPIP assert was **backwards until #300 (2 days ago)** | 6 layers; distortion tightens further | med |
| `preflop_50bb_6max.json` | No vs_squeeze; single raise size | Most defensible depth chart (keeps flats + raise tree); wide post-#295 RFI at 50bb unmeasured | Less shadowed (most band >15bb); veto/#299 on surviving cells | med |
| `preflop_100bb_hu.json` | No min-raise/limp tree; single raise size | Spot-checks sane (no trash-jam bug); CANNOT be linted (vocab mismatch raise_3bb vs raise_2.5bb) | push_fold_hu at ≤15bb; all-in veto; **#299 does NOT fire HU** | med |
| `postflop_strategies_3bp.json` | No aggressor flag; derived not solved | Generated then **CUT FROM PLAY** (neutral on SNG gate); neutrality verdict stale (weak harness) | NOT LOADED — fully inert in prod | med |
| `postflop_strategies_low_spr.json` | Only (SRP, low); derived | Same: shipped then CUT (neutral); commitment shape plausible; verdict stale | NOT LOADED — inert; rides degrade + postflop_commit | med |
| `push_fold_6max.json` | Only decides ~17%→98% (reshove on); no BB-unopened/multi-jammer/cold-caller; bb_vs_late no 4BB | v1 published-Nash APPROX (4BB + reshove cells [L]); reshove validated, unopened/caller NOT | reshove triple-gated; **caller tables dead weight** (veto fires first) | med |
| `postflop_strategies.authored.json` | Same footprint as live | Deliberately too passive (c-bet 39.7%) — "wrong by design"; only valid as rebalance source | N/A — not loaded; reaches play via rebalance only | low |
| `preflop_100bb_6max_wider_rfi.json` | Missing vs_squeeze (only diff from base) | REDUNDANT — byte-identical to base; not in ARCHETYPE_WIDTH_TABLE; can silently drift | N/A — never selected in prod | low |
| `push_fold_hu.json` | None material (full HU short-stack tree) | None — GTO-grade vs HUNE/HRC anchors; only caveat no-ante | PARTIAL — not short-circuited (distortion can reshape); bb_vs_jam shadowed by veto | low |
| `push_fold_equity_matrix.json` | None (full 169×169) | None — internally consistent; seeded MC ±0.3% noise | N/A — build-time cache, never read at runtime | low |

## 4. Prioritized Re-Validation Plan

Run in this order. The single biggest theme: **re-run everything that touches fold equity against the now-folding `Punisher_clone`**, because every pre-2026-06-11 result used a clone that never folded.

1. **`_apply_exploitation` headline (+22.5 bb/100).** Harness: `exploit_bb100` (CRN-paired, TAG hero, 8k hands × ≥3 seeds). Opponents: the **fixed folding `Punisher_clone`** as the precision-rewarding "reg" **plus** the old CallStation for continuity. Confirms/refutes: whether the +22.5 generalizes beyond a non-folding station — the log itself flagged "those aren't regs." **MOST SUSPECT** result in the set: measured vs a non-folder on an older codebase before #295/#299/#300. This is load-bearing for every offset-riding layer.

2. **`_apply_relationship_modifier_to_offsets` (ON by default, never measured).** Harness: `exploit_bb100` / `champion_challenger`, A/B `apply_relationship_modifier=True vs False` **with seeded heat/respect state** (the handoff's suggested A/B was never run). Confirms/refutes: whether a non-identity multiplier on the EV-critical offsets (applied pre-clamp) helps, is neutral, or inverts the exploitation edge. Highest *unknown-validation* risk — zero EV measurement has ever existed, yet it ships ON and mutates the +22.5 path.

3. **`overbet_context` value overbets + river_bluff/T2 (de-face-up thesis).** Harness: `ab_node_attribution` / `simulate_bb100+probe`. Opponents: fixed `Punisher_clone` folder **AND** the oracle sizing-reader (perfect-read upper bound). Confirms/refutes: the "never negative, +13 vs reg" claim vs a real folder, and re-prices the known **−22 bb/100 face-up cost** now that the regime gate (fold_to_big_bet≥0.6) can actually be satisfied. Suspect because the "reg" was a non-folder and the river-bluff numbers came from a synthetic oracle + pre-fix clones.

4. **`multistreet_context` H1 barrel (+3.33 vs jeff).** Harness: `simulate_bb100+probe` vs fixed `Punisher_clone` + `measure_passivity` for barrel-continuation rate; gate via `champion_challenger` CRN. Confirms/refutes: the barrel's fold-equity component, which was *never priced* because the jeff_clone never folded. The doc pre-registered jeff_clone as the control — the 2026-06-11 fix changed that control underneath the +3.33.

5. **`math_floor` call-off-all-in fix (new 2026-06-11, unit-test only).** Harness: `exploit_bb100` re-running the Finding-2 committed-fold exploit against a pot-committing adversary + `sng_runner` for the short-stack regime where it co-fires with the new reshove path + `simulate_bb100` to catch any new −EV forced jams from the call-off branch. Confirms/refutes: that the committed-fold exploit (~1.4% of prod hands) is actually closed and the call-off branch doesn't over-jam. Suspect because diagnosed on an older codebase and never sim-validated.

6. **`push_fold_6max` unopened + caller ranges (bb/100 = explicit TODO).** Harness: `sng_runner` / `simulate_bb100` with `--start-bb {8,10,12,15}` against a **now-folding** clone field. Confirms/refutes: the win-rate of the brand-new non-reshove 6max path (only routing-coverage exists). Note the caller tables (bb_vs_sb/bb_vs_late) are likely **dead weight** — the #271 all-in veto fires first; confirm before investing in them.

7. **Postflop personality stack — `_layer_spot_tendencies` (post + pre) + `defense_floor`.** Harness: `exploit_bb100` vs **both** a folding and a calling field (the sign of over_bluff/sticky flips between regimes); `measure_passivity` for WTSD-band on sticky 0.55; re-run the 1000×5 sim that rejected the jam row for defense_floor. Confirms/refutes: whether "over_bluff free vs callers" survives now that the clone folds, and whether the preflop+vs3bet-exploit **stacking** on vs_3bet composes sanely (one widens via defend_3bet, one folds via the knob). Suspect: late-May, old codebase, pre-regen, and the now-folding clone is exactly the regime flip that could turn "free" into a cost.

8. **`_apply_position_blindness` ($2 weak_fish leak −13.5 @40bb).** Harness: re-run the `StationPBlind − Calling Station` paired isolation vs the **now-folding** field at 40bb and 100bb. Confirms/refutes: that the fish still drains in-band into the cells the chart regen (#295/#296/#300) shifted. Oldest validation in the set; narrow blast radius (one $2 tier) but real economy impact.

9. **`_apply_vs3bet_bluff_exploit` (field-wide @0.5, worked-example only).** Harness: `exploit_bb100` — **must explicitly set `vs3bet_exploit`** (it no-ops in sims via getattr 0.0). Recompute β per node off the **post-regen** charts first, then verify the K9o worked example still holds. Broadest blast radius (field-wide ON), zero bb/100 number.

10. **`value_override` preflop looseness-scaled threshold (LAG +56, undated).** Harness: `champion_challenger`. Confirms/refutes: the only positive EV figures here predate the vs_4bet stub-chart fix (#271-#276) and the regen — "strong hand vs aggressor" is exactly what those charts now handle differently. Do not trust LAG +56 at face value.

11. **`tilt_conditioning` band-compliance under spike (just promoted prod-on).** Harness: `archetype_mixedfield_probe` with the maniac **forced into tilt** + `tilt_ev_probe`/`tilt_signature_probe` for EV direction. Confirms/refutes: that a tilt spike doesn't push the maniac (already AT its 4-bet/fold-to-3bet band edges) out of band, since `reraise_max_per_action_shift` is choked to 0.01 pending the regen.

12. **Add postflop lints (infrastructure, not a sim).** Port `lint_weights_sum` / `lint_legal_vocab` / `lint_completeness` to the 2,160-node postflop charts (currently **zero lint coverage**), and add a CI check that `live == rebalance(authored)`. Extend the strategic lints (fourbet_band, cliff_band) to the 6 width charts — `loose_mid` (lag) is the most likely undetected band violation.

## 5. Quick Wins / Likely-Dead

**Quick wins:**
- **Postflop lints** — cheap to add (`weights_sum`/`legal_vocab`/`completeness` analogs); the live postflop chart and all width/depth charts are currently un-linted. Add the `live == rebalance(authored)` drift check.
- **25bb BB-defense artifact** — the "100% jam / 0% flat" at 25bb is almost certainly a generator bug, not a calibrated range; a one-line poker-eyes fix to `generate_depth_charts.py` (allow flatting at 25bb) before any sim.
- **`weak_station` vs_3bet inversion** (33.9% < station 43.1%) — likely a generator mis-tune; verify and fix without a sim.
- **Update stale docs** — `LOOKUP_TABLE_PROVENANCE.md` "depth-chart footgun" note is now wrong (depth RFI is byte-identical to the *wide* base post-regen); base chart meta still claims "+17bb/100" / 2026-05-24.

**Likely-dead / production-inert (don't spend sim budget unless re-enabling):**
- `postflop_strategies.authored.json` — not loaded; rebalance source only.
- `postflop_strategies_3bp.json` / `postflop_strategies_low_spr.json` — generated, cut from play, NOT loaded; inert (the low-SPR one is the natural challenger chart *if* you A/B the new push/fold-vs-degrade-ladder interaction).
- `preflop_100bb_6max_wider_rfi.json` — **deletion candidate**: byte-identical to base minus vs_squeeze, not in `ARCHETYPE_WIDTH_TABLE`, risks A/Bs that think they test "wider RFI" but test "base minus vs_squeeze."
- `push_fold_6max` caller tables (bb_vs_sb/bb_vs_late) — dead weight; #271 all-in veto decides those spots first.
- All **OFF** tilt flags (PERSISTENCE / TELEGRAPH / ERRATIC / SIGNATURE) and `air_barrel` / H2 fold-barrel / `sizing_defense` — zero live risk; only validate before any enable decision. TELEGRAPH is frequency-neutral so has no EV to measure at all.
- `push_fold_equity_matrix.json` — deterministic seeded cache; staleness ≈ zero; treat as infrastructure.

---

## Verification addendum (2026-06-12) — the audit's own "quick wins" re-checked

Before acting on §5, each quick win was verified against the code/data (the same
don't-trust-unverified discipline applied to the audit itself). Most did NOT survive:

| Audit quick-win claim | Verdict | Evidence |
|---|---|---|
| Add postflop lints (zero coverage) | **TRUE — DONE** | `lint_postflop_{nonempty,weights_sum,legal_vocab}` added; 8,640 nodes now linted, all pass |
| `wider_rfi` byte-identical to base → delete | **FALSE** | `diff` says DIFFERENT; it's used by `champion_challenger.py:119` as a comparison chart — not a deletion candidate |
| 25bb BB-defense "100% jam / 0% flat" artifact | **OVERSTATED** | aggregate vs_open is fold 77.6 / jam 17.3 / **call 5.2** — it does flat; jam-lean at 25bb is plausibly deliberate, not an obvious bug. Defer to re-validation |
| `weak_station` vs_3bet inversion (< station) | **UNCONFIRMED** | 37.3% vs 47.2% continue is real but relative to each chart's own (different) open range — needs combo-level analysis, not a blind fix. Defer to re-validation |
| Stale `+17bb/100` base-chart meta / provenance note | **PARTLY** | meta note IS stale (pre-#295/#299/#300) but lives in a generated JSON (fix belongs in the generator); the "footgun note is wrong" claim is itself unverified (depth charts may not have been regenerated). Deferred |

**Meta-lesson:** the audit fan-out produced plausible-but-partly-wrong claims —
exactly the failure mode this whole exercise exists to catch. Treat the matrix
above as a *prioritized hypothesis list*, not ground truth; verify each cell
before acting. The only blind-safe action was the additive postflop lints.

---

## Re-validation results — Batch 1: exploitation (2026-06-12, Hetzner ccx63)

Re-ran the #1 most-suspect claim — `_apply_exploitation`'s "+22.5 bb/100" — on a
fresh box with current code, via `exploit_bb100 --change exploitation` (paired
CRN, 8000 hands × 3 seeds), across three opponent regimes. The folding
`Punisher_clone` (post-clone-fix) is the realistic-field test the original never had.

| Field | TAG edge (95% CI) | LAG edge | hands flipped |
|---|---|---|---|
| **old extreme station** (CallStation/FoldyBot, vpip~1.0) | **+10.3** [+4.5,+16.0] ✅ | **+9.9** [+4.1,+15.8] ✅ | ~1.9% |
| **competent** (GTO-Lite/ABCBot) | +3.6 [−2.7,+10.0] ➖ | +4.8 [−1.5,+11.2] ➖ | ~1.8% |
| **realistic folding field** (Punisher_clone×2 + Jeff_clone×2) | −0.1 [−0.2,+0.1] ➖ | +0.0 [0,0] ➖ | **0–1 / 24000 (0.0%)** |

**Verdict — the audit was right; the headline is stale + regime-specific:**
- The "+22.5" does NOT reproduce. Vs the same extreme-station type it's now **+10.3**
  (the #295/#299/#300 chart regen roughly halved it). CI-clear positive ONLY there.
- Vs a competent field it's **marginal** (+3.6/+4.8, CI spans 0).
- Vs a realistic folding field exploitation **does not even fire** (0% of hands
  flipped) → contributes ~nothing. Jeff_clone (vpip 0.39) is a mediocre reg, not a
  vpip-1.0 station, and Punisher folds correctly — neither crosses the exploitation
  detector's (extreme) thresholds.

**Implication:** `_apply_exploitation`'s measured value is entirely concentrated on
cartoonish (vpip≈1.0) opponents. Against anything resembling a believable player it
is inert. For the "hard for a competent human" goal it currently adds ≈0. **Open
follow-up:** is the 0%-fire-vs-clones *correct* (clones genuinely aren't exploitable)
or a *gap* (the detector only triggers on extremes and misses moderate leaks)? That
decides whether exploitation needs a lower-threshold tier or is simply a fish-only tool.

Batches still pending (next box session, proper probes written first): relationship
modifier (ON, never measured), multistreet H1 barrel + overbet (vs the folding field),
math_floor call-off, push_fold_6max unopened/caller bb/100.

---

## Follow-up — why exploitation flips 0% vs the clones (2026-06-12, 4-agent probe)

Investigated the Batch-1 open question. Reconciled against the **live-measured**
data (one agent instrumented a real TAG-vs-clone sim; treat that as authoritative
over agents that reasoned from the nominal profiles). The 0% is a **combination of
detection gaps + a magnitude throttle — NOT "clones are unexploitable."**

**Live-measured detection (TAG hero, ~1500 hands):**
- Punisher_clone: vpip/vol ~0.17 → fires `tight_nit` only — the WRONG tool (it widens
  hero's *opens*; does nothing about Punisher's postflop play). Its authored
  `fold_to_cbet=0.70` does **not** appear live (observed ~0.06–0.13, cbet_faced ~17) →
  `high_fold_to_cbet` never fires.
- Jeff_clone: fires **nothing** — vpip/vol ~0.35 sits in the dead zone between nit
  (<0.30) and station (>0.70); his real leak is *postflop* (AF_postflop ~0.30, wtsd
  0.59) which the station gate can't see.
- all-in ~0 for both → no aggression detector.

**Magnitude throttle (link 3):** even where a pattern fires (tight_nit), the offset =
`base × adaptation_bias × confidence × intensity` shrinks to a ~0.05–0.12 logit shift
→ flips only ~5–9% of hands at reg bias 0.5 (≤18% even at an unreachable bias 1.0),
rounding to ~0 on the CRN-paired measure. The L1 clamp/budgets are **slack** (never
binding); the multiplier is the ceiling. Detection ≠ behavioral change.

**Two code-certain detector gaps** (real, but see caveats):
1. `high_fold_to_cbet` is HU-gated (`active_opponent_count==1`, exploitation.py:1632)
   → can't fire in a multiway field even if detected.
2. `_is_hyper_passive` gates on **global** `aggression_factor` (L681), not
   `aggression_factor_postflop` → masks postflop calling-stations like Jeff.

**⚠ Verification catch:** the agent synthesis proposed "Fix A: wire a multiway c-bet
trigger to `high_fold_to_cbet` to close Punisher" — but that's premised on Punisher
being *detected* as a high-fold-to-cbet, which the **live data refutes** (observed
0.06, not 0.70). The authored clone stat ≠ its actual play vs this hero (a clone-
fidelity gap) + the hero rarely c-bets it (~17 samples). So Fix A would not fire
without first fixing the upstream stat. Likewise Fix B (postflop-AF gate) won't catch
Jeff alone: his live vpip/vol ~0.35 is still below the 0.70 hyper_passive cutoff.

**Verdict / decision needed:**
- It's a real gap (the clones DO have leaks), but the proposed quick fixes are each
  premised on detection that doesn't currently fire live — don't apply blindly.
- The deeper finding: **the magnitude throttle makes exploitation a *light nudge*,
  not a behavioral swing**, even with perfect detection. Before tuning, make the
  design call explicitly: is exploitation meant to be a light edge on a strong base
  (then A/B detection-reachability is the scope), or a real exploit engine (then it
  needs a hard-override path like the value/all-in-veto layers, + lower thresholds)?
- New side-finding: the **punisher clone's authored `fold_to_cbet` doesn't manifest
  in play** — the clone isn't a clean test bed for c-bet exploitation; revisit clone
  fidelity (same theme as the fold-to-3bet fix) before trusting clone-based c-bet A/Bs.

---

## First-principles test — the canonical "stop bluffing vs a station" exploit (2026-06-12)

Jeff's basic exploit: vs a player who always calls, stop bluffing + value-bet
more. Measured the BEHAVIOR directly (`experiments/exploit_behavior_probe.py`):
TAG hero vs a pure CallStation, exploitation ON (strength 1.0) vs OFF (0.0),
postflop actions split by hand class.

| Field | bluff rate (air-aggression) | value rate (strong/nuts) |
|---|---|---|
| **HU OFF** | **59.9%** | 95.0% |
| **HU ON** | **59.8%** (Δ −0.1pp) | 97.2% (Δ +2.1) |
| 6-max OFF | 14.4% | 94.0% |
| 6-max ON | 14.1% (Δ −0.3pp) | 96.2% (Δ +2.3) |

**The exploit does NOT work.** The bot bluffs ~60% (HU) with air into a calling
station and exploitation changes that by −0.1pp (noise). Value-betting is already
near-ceiling (94–97%) so there's no headroom there either.

**It is NOT a detection or enablement failure** (the prior clone result was). Vs a
clear CallStation, instrumentation shows the chain works up to the last step:
`archetype_name='tag'` → `is_value_vs_station_enabled` **True** (5398/5398);
`compute_value_vs_station_intensity` returns **1.0** (maxed, 3764/3846); the
`bluff_reduction` rule's hand-gate (air) fires. The break is **offset→behavior**:
`_apply_bluff_reduction` emits a `-0.20 × multiplier` logit nudge on bet/raise
(+0.10 check), and after the `adaptation_bias × confidence × strength` multiplier
the realized shift is small enough that the sampled action almost never changes —
**detection at full strength yields ≈0 behavioral change.**

**Conclusion / fix direction:** exploitation as architected is a *soft logit nudge*
that does not translate into behavior even at max intensity vs the clearest target.
For it to be a real, measurable capability (Jeff's bar), the highest-confidence
exploits need a **hard-override / much larger magnitude** path — like the
`value_override` and facing-all-in-veto layers already use (they hard-set the
action) — not a small additive offset. The right gate to prove the fix is THIS
behavioral probe (bluff% must actually drop vs a station), then bb/100. The
"light nudge vs real exploit engine" design call is now answered by data: the
light nudge produces nothing.
