# Commits in this file are skipped by `git blame` so bulk-format/tooling commits
# don't bury the actual authorship of each line.
#
# To make blame skip these locally:
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
# GitHub picks this up automatically.

# style: apply ruff --fix + ruff format across the codebase (2026-05-25)
0eef948c
