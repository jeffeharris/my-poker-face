"""Chip-economy observability primitives.

Thin wrappers around the v93 ledger surface. Call sites import from
here rather than directly from the repository so future enforcement
(central bank v1) can change the implementation without churn.
"""
